# Chronopolis dependencies (Python 3.11-3.13; 3.14+ has no moderngl/manim wheels
# yet). Also needs ffmpeg on the system PATH.
numpy>=2.5.1
manim>=0.20.1
tkinterdnd2>=0.4.0   # optional: enables file drag-and-drop into the GUI
